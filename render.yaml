services:
  # Backend Service
  - type: web
    name: s65_hrithik_capstone_hopeplates-1
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: PORT
        value: 10000

  # Frontend Service (Static Site)
  - type: static
    name: s65_hrithik_capstone_hopeplates-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: s65_hrithik_capstone_hopeplates-1
          property: url
